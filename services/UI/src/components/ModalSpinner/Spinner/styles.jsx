import { makeStyles } from "@material-ui/styles";

export default makeStyles(theme => ({
    containerSpinner: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
    },
    spinner: {
        width: '20%'
    }
}));
